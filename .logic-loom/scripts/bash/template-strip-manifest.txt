# =============================================================================
# LogicLoom — template strip manifest (harness-dev → sanitized public main)
# -----------------------------------------------------------------------------
# SINGLE SOURCE OF TRUTH for the "harness-dev artifact" concern. Consumed by:
#   - strip-harness-dev.sh   (REMOVES / stubs these paths when building a
#                             sanitized snapshot from a dev-main checkout)
#   - leak-guard.sh          (ASSERTS these paths are ABSENT from the snapshot;
#                             exit 1 on any leak)
#   - sanitization-audit.sh  (Check 7 delegates to leak-guard.sh, but only when
#                             this manifest + leak-guard.sh are present)
#
# TOPOLOGY (decided 2026-06-15): the customer template `main` is PUBLIC, so
# dev-main lives in a SEPARATE PRIVATE repo and this manifest + the whole release
# plumbing are themselves harness-dev — they are STRIPPED so they never ship to
# customers. The promotion build runs in the private repo; only single-parent
# sanitized snapshots are pushed to the public template.
#
# This is ORTHOGONAL to sanitize-for-template.sh (per-PROJECT generated artifacts)
# and to sanitization-audit.sh Checks 1-6 (assert the ORIGINAL ioun-ai project +
# absolute /workspaces paths were scrubbed). This manifest covers OUR harness-
# development record.
#
# TRACKED-CONTENT MODEL: the stripper and leak-guard operate on GIT-TRACKED
# content (`git ls-files`), NOT the working-tree filesystem. Gitignored runtime
# state (memory tiers' working/recall/archival contents, .logic-loom/logs,
# .loom-worktree-env, egg-info) is never tracked, so it never enters a CI
# checkout and needs no strip entry — only its tracked `.gitkeep` survives.
#
# Grammar (one entry per line; `#` begins a comment):
#   <path-or-glob>     -> REMOVED by stripper; asserted ABSENT by leak-guard
#   stub: <path> :: <template>
#                      -> CONTENT-REPLACED with <template> (a repo-relative path,
#                         conventionally under .logic-loom/templates/); asserted
#                         to carry NO harness-dev markers (not absent).
#                         The `:: <template>` field is REQUIRED and there is NO
#                         default: the stripper ABORTS on a `stub:` entry with no
#                         template, or whose template file is missing. Shipping
#                         the WRONG stub is worse than failing the release, and a
#                         default is exactly how the wrong stub gets shipped
#                         silently (the stripper used to hardcode the VISION stub
#                         for every `stub:` target).
#   warn: <path-glob>  -> NOT stripped; leak-guard emits a WARNING (non-fatal).
#                         Use for deferred decisions so nothing silently ships.
# Globs are matched against `git ls-files` output (tracked paths only).
# =============================================================================

# --- Root product vision: NAME is the customer's slot, CONTENT is our harness
#     roadmap (cites loom-migration / PR #56 / freeze residual). Ship the stub so
#     the constitution / CLAUDE.md / AGENTS.md references resolve on a fresh
#     clone; /initialize-project then fills it in place. ---
stub: VISION.md :: .logic-loom/templates/project-vision-template.md

# --- Harness maintenance backlog (SSOT Level 0): the FILE is machinery a cloner
#     should inherit -- the todo-architecture policy points at this exact path,
#     so a missing file breaks that reference on a fresh clone. The CONTENT is
#     ours: dev commit SHAs, a private sibling repo, our release topology, and
#     open governance decisions. Ship the stub (header + grammar + empty Items),
#     never our items. Same shape and same reason as VISION.md above. ---
stub: .logic-loom/memory/backlog.md :: .logic-loom/templates/project-backlog-template.md

# --- Release plumbing SCRIPTS: maintainer-only. These contain identity markers
#     as detection patterns / origin provenance by design, so they must NEVER
#     ship. Stripped here at PROMOTE time. The build runs sanitize-for-template.sh
#     BEFORE strip-harness-dev.sh (so it can do its job before being removed), and
#     runs sanitization-audit.sh + history-scrub.sh from a PRESERVED copy outside
#     the tree (/tmp/loom-audit-tools) — so all of these can be stripped from the
#     shipped tree. (promote-to-main.yml is handled differently — see below.) ---
.logic-loom/scripts/bash/strip-harness-dev.sh
.logic-loom/scripts/bash/sanitize-for-template.sh
.logic-loom/scripts/bash/sanitization-audit.sh
.logic-loom/scripts/bash/leak-guard.sh
.logic-loom/scripts/bash/template-strip-manifest.txt
# --- Release DRIVER (maintainer-only): the /promote command (a loom-maintenance
#     plugin command) + its bridge wrapper + the version-stamp helper. Customers
#     do NOT promote LogicLoom's template from their own project, so these never
#     ship — stripped at promote. The sanitized-tree bridge test scans
#     .claude/commands files (the wrapper is gone, so it stays green); loom-
#     maintenance plugin.json keeps a now-3rd entry, which no test validates
#     against files. NOTE: .claude/commands/.bridge-manifest.json SHIPS and is a
#     DERIVED index of .claude/commands/*.md — removing the wrapper alone would
#     leave it advertising /promote. strip-harness-dev.sh re-derives it (via
#     sync-plugin-commands.sh prune) after this manifest is applied. ---
.logic-loom/scripts/bash/bump-version.sh
plugins/loom-maintenance/commands/promote.md
.claude/commands/promote.md
.logic-loom/scripts/bash/history-scrub.sh
.logic-loom/scripts/bash/history-scrub-rules.json
#
# NOTE: .github/workflows/promote-to-main.yml is intentionally NOT stripped here.
# In the SINGLE-PUBLIC-REPO model the release workflow must live on the default
# branch (main) to be `workflow_dispatch`-able — stripping it would make our own
# CI release loop impossible. It carries no identity markers (it resolves the repo
# via github.repository and runs the maintainer scripts from a dev-main checkout),
# so it ships safely on OUR main. It is removed from a CUSTOMER's project instead,
# at /initialize-project time (project-initialization skill + init-project.sh
# cleanup), so a customer's repo is not cluttered with our release tooling.

# --- Harness-dev documentation record (tracked) ---
.docs/features          # harness-dev feature records, if any (defensive)
.docs/archive           # archived feature-003 plans + upstream-sync reports
.docs/reports           # references the external ../../kelleysd.com private repo
.docs/design            # marketplace-mcp-middleware.md (REMOVED / historical)
.docs/guides/dev-main-template-split.md   # OUR release runbook (maintainer-only)
.logic-loom/plans       # our dev planning (docker-mcp-toolkit-integration.md)
CHANGELOG.md            # the harness's own version history (v6.2 dev-loop, etc.)

# --- Repo-root `artifacts/`: the CONVENTION ships (documented in CLAUDE.md's
#     directory structure and .docs/policies/file-structure-policy.md), the
#     CONTENTS are ours. Today that is two hand-authored LogicLoom pages —
#     artifacts/harness-graph.html ("What the harness is actually made of") and
#     artifacts/logicloom-vision.html ("Vision, Findings & Record", carrying our
#     decisions, verified problems and open evidence gaps). Same class as
#     .docs/reports and .docs/design above, which are already stripped wholesale
#     — and until now `artifacts/` was in NO list at all, so both files were
#     shipping to customers verbatim.
#     WHOLESALE, not `artifacts/*.html`: a glob would silently ship the first
#     non-HTML artifact we drop in there. Nothing is stubbed and no .gitkeep is
#     shipped, so the directory simply does not exist in a customer clone —
#     the same treatment as `web/`, which the policy documents without shipping.
#     A customer creates it the first time they have an artifact to put in it.
artifacts

# --- GENERATED project-graph index (derived, repo-specific) ---
#     .logic-loom/graph/graph-bridge.jsonl is a DERIVED index of THIS repo's
#     markdown corpus, produced by build-graph-bridge.sh (/graph build, /finalize).
#     It therefore embeds OUR corpus: node ids + titles for .docs/archive/,
#     .docs/reports/ (incl. the kelleysd.com private-repo path) and other paths
#     that are themselves stripped above — i.e. it re-introduces exactly what the
#     entries above remove. It is also useless to a customer: an index of someone
#     else's repo. Strip the whole directory (covers sibling generated exports,
#     e.g. .logic-loom/graph/*.json). Customers regenerate it from their OWN repo
#     with `/graph build` (or automatically at `/finalize`); build-graph-bridge.sh
#     mkdir -p's the directory, and tests/contract/test_graph_bridge.sh check (c)
#     validates the seed only "if present". If a static graph VIEWER is ever
#     shipped under .logic-loom/graph/, re-scope this entry to the .jsonl/.json
#     artifacts instead of the directory.
.logic-loom/graph

# --- LogicLoom-contribution scaffolding (about contributing TO the harness, not
#     a customer's own project) ---
.github/ISSUE_TEMPLATE
.github/pull_request_template.md

# --- Tracked runtime/agent records that slipped into git (belt-and-suspenders:
#     leak-guard fails loudly if any are tracked on the snapshot) ---
.docs/memory            # search-log.jsonl query telemetry
.docs/governance/audit  # session audit logs
.docs/agents/*/*/decisions   # runtime agent decision JSON (any department)
.loom-active-feature    # generated active-feature marker

# --- Intentionally NOT stripped (documented so reviewers see the decision):
#   README.md                 : init-project.sh archives -> FRAMEWORK_README.md
#   CLAUDE.md / AGENTS.md      : harness operating instructions (ship)
#   .docs/architecture/*       : authoritative contract docs (ship)
#   .docs/policies, guides, references, troubleshooting : downstream-facing (ship)
#   .logic-loom/templates/*    : scaffolding incl. project-vision-template (ship)
#   .logic-loom/memory/constitution.md : governance core (ship)
#   plugins/** , .claude/hooks/** : the harness itself (ship)
#   plugins/loom-orchestrator-hook/config/verification-intent.conf : cross-check
#                                disposition trigger phrases (ship; covered by
#                                plugins/** — listed explicitly as an audited
#                                decision so the verification-intent nudge reaches
#                                customer copies)
#   tests/** , .logic-loom/tests/** : governance-integrity regression suite (ship)
#   .github/workflows/leak-guard.yml  : self-contained public backstop (ship;
#                                stripped from a CUSTOMER project at init)
#   .github/workflows/promote-to-main.yml : maintainer release loop — must be on
#                                main to be dispatchable (ship; marker-free;
#                                stripped from a CUSTOMER project at init)
#   .github/workflows/release-tag.yml : auto-tags vX.Y.Z on a release merge to
#                                main — must be on main to run on main pushes
#                                (ship; marker-free; stripped from a CUSTOMER
#                                project at init, like promote-to-main.yml)
#   .github/workflows/branch-topology-guard.yml : fails any PR into main whose
#                                head branch is not release/vX.Y.Z. MUST ship —
#                                a `pull_request` workflow is evaluated from the
#                                BASE branch, so stripping it would delete the
#                                only copy that can ever run (ship; marker-free;
#                                stripped from a CUSTOMER project at init, like
#                                promote-to-main.yml)
#   .github/workflows/plugin-tests.yml: validates the shipped harness (ship)
#   features/ , specs/         : empty customer workspaces (ship; .gitkeep only)
#   .sdd-sync-ref              : /update-framework upstream pointer (ship)
#   .logic-loom/config/framework-upstream.conf : /update-framework upstream URL,
#                                stamped at release from github.repository (ship)
# =============================================================================
