# LogicLoom framework upstream (FETCH-ONLY) — stamped at release.
# /update-framework fetches this ad-hoc into refs/loom-upstream/main;
# it is NOT a git remote and is never pushed to. Your commits go to origin.
LOOM_UPSTREAM_REPO="kelleysd-apps/LogicLoom"
LOOM_UPSTREAM_URL="https://github.com/kelleysd-apps/LogicLoom.git"
