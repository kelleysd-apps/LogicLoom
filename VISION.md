# PRODUCT VISION — <Your Product>

> **Foundational** product north-star — a distinct artifact *class*, not another
> vision doc. There is exactly ONE per project, it lives the life of the project,
> and it is a **peer to the constitution**: the constitution governs *how* work
> is done (the safety/quality floor); this VISION declares *what* the project is
> and *why* it exists (the direction the constitution defers to). It is NOT a
> per-feature vision (`features/<name>/vision.md`, single-shot and ephemeral).
>
> A **LIVING** document — replace every placeholder, keep it current, generate
> new tasks FROM it. Scaffolded/filled by `/initialize-project` alongside the
> constitution; read together with `CLAUDE.md`, `AGENTS.md`, and project memory.

**Status**: Living · **Owner**: <you> · **Last reviewed**: <date>

## North Star
<One sentence: the change this product makes in the world.>

## Why we exist (the bet)
<The core hypothesis this product is testing — the wager behind the work.>

## Who this is for
<Primary users / customers and the job they hire this product to do.>

## What success looks like
- **Qualitative**: <what "good" feels like for a user>
- **Quantitative**: <measurable targets / signals>

## Strategic Pillars (each pillar seeds tasks)
1. **<Pillar>** — <why it matters; what it unlocks>
2. **<Pillar>** — <…>

## What this is NOT (explicit non-goals)
- <a thing you are deliberately not doing>

## Open Threads (the live backlog — generate tasks from here)
- <open question or bet to resolve>

## Keeping this document alive
Review at every `/retro`. A fresh session should be able to re-orient from
`VISION.md` + memory + `CLAUDE.md` alone. When a pillar is delivered or a thread
resolves, update this file in the same change.
