# sdd-domain-CHANGEME
Template domain plugin. Copy this directory and customize for your domain.

## Setup
1. Copy: `cp -r sdd-domain-template sdd-domain-yourname`
2. Update plugin.json (name, description, keywords)
3. Replace template skill and agent with your domain content
4. Test: `claude plugin install ./sdd-domain-yourname`
