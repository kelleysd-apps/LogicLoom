---
name: git-push
description: Complete git workflow — commit, push, PR creation with merge conflict resolution. Requires user approval at every stage.
model: opus
---
# /git-push Command
Activate the git-push-workflow skill. All git operations require explicit user approval (Principle VI).
