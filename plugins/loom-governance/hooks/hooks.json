{
  "hooks": [
    {
      "event": "PreToolUse",
      "matcher": {
        "tool_name": "Write|Edit|MultiEdit|NotebookEdit|Bash"
      },
      "command": "${CLAUDE_PLUGIN_ROOT}/hooks/scripts/protect-governance-files.sh",
      "description": "Protect the governance surface (hooks/settings/constitution/governance.conf) from edits: subagent deny, main ask"
    },
    {
      "event": "PreToolUse",
      "matcher": {
        "tool_name": "Bash"
      },
      "command": "${CLAUDE_PLUGIN_ROOT}/hooks/scripts/subagent-git-guard.sh",
      "description": "Deny ALL git operations from subagents (agent_id present); git is main-agent + user-request only (Principle VI)"
    },
    {
      "event": "PreToolUse",
      "matcher": {
        "tool_name": "Bash"
      },
      "command": "${CLAUDE_PLUGIN_ROOT}/hooks/scripts/git-safety-gate.sh",
      "description": "Gate git mutations from the main agent requiring explicit user approval (Principle VI)"
    }
  ]
}
